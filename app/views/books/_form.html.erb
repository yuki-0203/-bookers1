<h2>New book</h2>

<% if @book.errors.any? %>
 <div class="alert-danger">
  <%= @book.errors.count %>errors prohibited this book from being saved:
 <ul><% @book.errors.full_messages.each do |message| %>
    <li class="alert-danger-list"><%= message %></li>
  <% end %>
   </ul>
  </div>
<% end %>

 <%= form_with model: @book, local:true do |f| %>
  
  <div class="field">
    <h4>title</h4>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <h4>Body</h4>
    <%= f.text_area :body %>
  </div>

  <div class="actions">
    <%= f.submit 'Create Book' %>
  </div>
<% end %>
